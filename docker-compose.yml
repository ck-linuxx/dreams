version: '3.8'

services:
  postgres:
    image: bitnami/postgresql:latest
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=dreams
      - POSTGRES_PASSWORD=dreams
      - POSTGRES_DB=dreams
